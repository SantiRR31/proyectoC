
import tkinter as tk
import customtkinter as ctk

def mostrar_inicio(frame_padre):
        etiqueta = ctk.CTkLabel(frame_padre, text="Bienvenido al Sistema", font=("Arial", 22))
        etiqueta.pack(pady=60)